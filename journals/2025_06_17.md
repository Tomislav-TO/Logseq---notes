public:: true

- ## Developer: [Tomislav Tomašković]
  
  ---
- ## Developer: [Tomislav Tomašković]
  
  ---
- ### **Tasks Completed**
- Helped debug MySQL/MariaDB user connection issues for Frappe/Press deployment
- Clarified how `@'YOUR-NETWORK-RANGE'` works in MySQL GRANT statements
- Verified which users and hosts exist in MySQL with:
  
  `SELECT user, host FROM mysql.user;`
- Tested MySQL access using:
  
  ```
  mysql -u press -p press_production -e "SHOW DATABASES;"
  ```
- Explained how to distinguish which user is being used when multiple users exist for different `host` values
- Diagnosed why login attempts with `frappe-user` or `press` may fail (wrong host or credentials)
- Confirmed `frappe-admin` **Linux system user** was created with:
  
  ```
  bash
  
  CopyEdit
  
  getent passwd frappe-admin
  ```
- Verified `frappe-admin` is **not a MySQL user** using:
  
  ```
  sql
  
  CopyEdit
  
  SELECT user, host FROM mysql.user WHERE user = 'frappe-admin';
  ```
- Provided SQL snippet to create `frappe-admin` as a MySQL user if needed
- Explained and tested access with:
  
  ```
  bash
  
  CopyEdit
  
  sudo su - frappe-admin
  ```
  
  ---
- ### **What I Found**
- MySQL has users:
	- `press` and `frappe-user` for both `localhost` and `10.0.%`
	- `frappe-admin` does **not** exist as a MySQL user (only as a Linux user)
- Access errors likely due to:
	- Connecting from wrong host (localhost vs network)
	- Incorrect password
- System user `frappe-admin` **does exist** with `/home/frappe-admin` and `/bin/bash` shell
  
  ---
- ### **Tomorrow’s Plan**
- Decide if `frappe-admin` should be added as a MySQL user
- If yes, create user with correct host IP or wildcard (e.g. `10.0.%`)
- Test access again from both local and remote machines
- Verify if this user needs full access or limited access
- Optionally configure `.my.cnf` for passwordless CLI access for system users
  
  ---
- ### **Blockers**
- Confusion between Linux users vs MySQL users
- Misunderstanding of how MySQL matches users based on host IP
- Possible password/host mismatch causing failed `mysql -u` login attempts
  
  ---
- ### **Tools Used**
- SSH (to access database server)
- MariaDB CLI for user/host inspection
- `getent passwd` for Linux user validation
- Shell commands like `mysql -u`, `su -`, and user management (`adduser`, `usermod`)