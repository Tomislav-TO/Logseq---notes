public::  true

-
- ## Developer: [Tomislav Tomašković]
  
  ---
- **Start Time:** ~10:30
  
  **End Time:** ~14:30
  
  **Total Hours:** 4
  
  ---
- ### ✅ Tasks Completed:
- Verified system status (Cloudflare tunnel, services, DNS resolution)
- Pulled latest backend/frontend code from GitHub repo
- Resolved local Git merge conflicts (related to domain configurations)
- Updated all domain references from staging/demo to production (`tomi.keystonix.app`)
- Restarted backend and frontend services with updated domain mappings
- Verified deployment of new business modules:
	- PDV, payroll, tourism, notifications, enhanced WALLE features
- Confirmed tunnel is routing correctly to internal services
- Applied domain-specific config across multiple env and code files
  
  ---
- ### 🧪 LeetCode Assignments Practiced:
- **#35 – Search Insert Position**
  
  → Implemented binary search to find or insert index in sorted array
  
  → Focused on achieving O(log n) runtime complexity
- **#69 – Sqrt(x)**
  
  → Solved using basic linear loop and discussed binary search optimization
  
  → Practiced rounding down behavior without built-in math functions
  
  → Explored control flow and loop logic in depth
- **#45 – Jump Game II (briefly reviewed)**
  
  → Discussed optimal greedy strategy and jump boundaries
  
  ---
- ### 🖥️ Current System Status:
- Application now returns HTTP 200 successfully (fixed prior redirect issue)
- New features and business modules deployed and operational
- Backend/frontend services running properly on updated codebase
- Tunnel config correctly points to active server instance
  
  ---
- ### ⚠️ Remaining Issue:
- **DNS configuration conflict:**
	- A/AAAA records are still taking precedence over CNAME
	- Manual cleanup needed in Cloudflare dashboard
	- Only CNAME pointing to tunnel should remain
	  
	  ---
- ### 🛠️ Tools Used:
- `git` for repo management and merge conflict resolution
- `ssh` for server login and manual service restarts
- `curl` for verifying HTTP routing and responses
- `sed` for batch-updating domain strings in code
- `npm` to rebuild and restart services
- `systemctl` to monitor tunnel service
  
  ---
- ### 🔜 Next Steps:
- User must clean up DNS A/AAAA records in Cloudflare
- Confirm tunnel-only routing using CNAME for production domain
- Optionally refactor sqrt logic to use binary search for performance
  
  ---
  
  **🕓 Log submitted at:** 11:10